/**
 * > place++;
 * < place--;
 * + array[place]++;
 * - array[place]--;
 * . System.out.print(Character.toString((char) array[place]));
 * , array[place] = (int) in.next().charAt(0);
 * [ while (array[place] != 0) {
 * ] }
 */
import java.util.Scanner;
class Main {
    public static void main(String[] args) {
    	Scanner in = new Scanner(System.in);
    	int[] array = new int[32000];
    	int place = 16000;
    }
}
